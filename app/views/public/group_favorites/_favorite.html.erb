<% if group.group_favorited_by?(current_customer) %>
    <%= link_to "♥",group_group_favorites_path(group), method: :delete, remote: true, class: "favorite-red" %>
<% else %>
    <%= link_to "♥", group_group_favorites_path(group), method: :post, remote: true, class: "favorite-black" %>
<% end %>
<%= link_to "#{group.group_favorites.count}お気に入り", group_group_favorites_path(group), class: "btn btn-favorite" %>