<div class='container px-5 px-sm-0'>
  <div class="row mt-5">
    <h2 class="midashi"><%= @customer.name %>のDM一覧</h2>
  </div>
  <div class='row mt-5'>
    <%= render 'public/rooms/index', rooms: @rooms %>
  </div>
  <%= paginate @rooms %>
</div>