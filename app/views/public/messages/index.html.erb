<div class='container px-5 px-sm-0'>
  <div class="row mt-5">
    <h2 class="midashi"><%= @customer.name %>の新着メッセージ</h2>
  </div>
  <div class='row mt-5'>
    <%= render 'public/messages/index', messages: @messages %>
  </div>
  <%= paginate @messages %>
</div>